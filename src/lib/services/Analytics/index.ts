// import analytics from "@react-native-firebase/analytics";

// export const logEvent = (eventName, payload = null) => {
//   analytics().logEvent(eventName, payload);
// };

// export const setLoggingUserId = (id) => {
//   analytics().setUserId(id);
// };
