import {showModal} from '../../helpers/navigation';
import {Screens} from '../../../navigation/screens';

export const handleDeepLink = ({url}) => {
  // if (!url) return;
  // let path = url.split("aqar.space/")[1];
  // if (path.match(/property\/.*/g)) {
  //   return showProperty(path.split("/")[1]);
  // }
};

export const showProperty = (id) => {
  // showModal(Screens.Property, {
  //   id,
  //   isModal: true
  // });
};
